<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.ifmo.android_2016.lesson2.loadimagedemo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".DebugApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExampleActivity1" />
        <activity android:name=".ExampleActivity2" />
        <activity android:name=".ExampleActivity3" />
        <activity android:name=".ExampleActivity4" />
        <activity android:name=".ExampleActivity5" />
        <activity android:name=".ExampleActivity6" />
    </application>

</manifest>